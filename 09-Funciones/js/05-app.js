function sumar(a, b) {  // a y b son parámetros
    console.log( a + b);
}

sumar( 2, 3 );  // 2 y 3 son argumentos

function saludar(nombre, apellido) {
    console.log(`Hola ${nombre} ${apellido}`)
}

saludar ("José Miguel", "Izquierdo");
saludar("Jose");